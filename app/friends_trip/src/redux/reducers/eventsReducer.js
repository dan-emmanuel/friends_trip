
const initState = {
    currentUser : {}
}


export const eventsReducer = (state=initState,action)=>{
    switch (action.type) {
        case '':
            
        break;
    
        default:
        return {...state}   
    }
}

export default eventsReducer